package sherwood.demo.structures.levels.event;

public enum Event {

    /**
     * called when the level should be reset (R)
     */
    reset,

    /**
     * called when the player dies
     */
    playerDeath
}
